package com.oscarcommerce.fw;

import org.openqa.selenium.WebDriver;

public class SearchFunctionHelper extends BaseHelper {
    public SearchFunctionHelper(WebDriver webDriver) {
        super(webDriver);
    }
}
